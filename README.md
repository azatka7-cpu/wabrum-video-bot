# Wabrum Content Bot 🎬

Telegram-бот для автоматической генерации видеоконтента для маркетплейса [Wabrum.com](https://wabrum.com).

## Как это работает

1. **CS-Cart API** — бот забирает новинки и популярные товары с маркетплейса
2. **Claude AI** — AI-стилист оценивает товары и генерирует креативные промпты
3. **KlingAI 3.0** — генерирует гиперреалистичные видеоролики по промптам
4. **Telegram** — владелец одобряет/отклоняет каждое видео

Бот работает в режиме **полуавтомата**: генерация автоматическая, публикация — по одобрению.

## Установка

### 1. Клонирование и виртуальное окружение

```bash
cd /home/ubuntu
git clone <your-repo-url> wabrum_bot
cd wabrum_bot
python3 -m venv venv
source venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
nano .env
```

Заполните все поля:
- `TELEGRAM_BOT_TOKEN` — получить у [@BotFather](https://t.me/BotFather)
- `TELEGRAM_ADMIN_IDS` — ваш Telegram User ID (через [@userinfobot](https://t.me/userinfobot))
- `CSCART_API_EMAIL` / `CSCART_API_KEY` — из панели администратора CS-Cart
- `KLINGAI_API_KEY` — из [KlingAI Developer Portal](https://klingai.com)
- `ANTHROPIC_API_KEY` — из [Anthropic Console](https://console.anthropic.com)

### 4. Тестовый запуск

```bash
python test_data.py   # Проверка БД и моков (без API-ключей)
python main.py        # Запуск бота (требуются API-ключи)
```

### 5. Установка как systemd сервис

```bash
sudo cp wabrum_bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now wabrum_bot
```

### 6. Проверка и логи

```bash
sudo systemctl status wabrum_bot
journalctl -u wabrum_bot -f
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Статус системы |
| `/generate` | Запустить генерацию вручную |
| `/queue` | Показать видео для одобрения |
| `/stats` | Статистика за 7 дней |
| `/help` | Справка |

## Архитектура

```
wabrum_bot/
├── main.py                  # Точка входа
├── config.py                # Настройки
├── database/
│   ├── db.py                # Схема БД (SQLite)
│   └── models.py            # CRUD-операции
├── services/
│   ├── cscart.py            # CS-Cart API
│   ├── claude_stylist.py    # AI-стилист (Claude)
│   ├── klingai.py           # KlingAI 3.0 API
│   └── scheduler.py         # Ежедневная автогенерация
└── bot/
    ├── handlers.py          # Telegram-хендлеры
    └── keyboards.py         # Inline-клавиатуры
```

## Расписание

Бот автоматически запускает генерацию каждый день в **09:00** по ашхабадскому времени (04:00 UTC).

## Лицензия

Проприетарное ПО. © Wabrum.com
